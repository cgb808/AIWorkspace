########################################
# ZenGlow AI Example Environment File
########################################

# Core
STRICT_ENV=false
DEV_FAKE_LLM=true
RAG_RETRIEVAL_MODE=disabled
DATABASE_URL=sqlite:///./dev.db

# (Optional) Redis (leave blank to skip)
REDIS_HOST=redis
REDIS_PORT=6379

# (Optional) Supabase (lazy init; leave blank if unused)
SUPABASE_URL=
SUPABASE_KEY=
SUPABASE_SERVICE_KEY=
SUPABASE_ANON_KEY=

# Persona / Misc (add as needed)

# DO NOT PLACE SECRETS (like Docker PAT) IN REPO .env; use local .env only.# Core service connectivity
CHROMA_HOST=chroma          # if running locally without docker set to 'localhost'
CHROMA_PORT=8000            # container internal port (Docker compose maps 8001:8000 host:container)

# Postgres (local dev only; credentials are non-sensitive defaults)
POSTGRES_HOST=db            # service name in docker-compose (not container_name)
POSTGRES_DB=indexer
POSTGRES_USER=indexer
POSTGRES_PASSWORD=indexer

# Optional: Supabase Cloud (do not commit real keys)
# SUPABASE_URL=
# SUPABASE_ANON_KEY=

REDIS_HOST=redis
REDIS_PORT=6379

# Collection / Vector config
CHROMA_COLLECTION=documents

# Local dev convenience:
# Copy this file to .env and adjust any secrets before starting docker-compose.

# XRDP
XRDP_USER=vscode
XRDP_PASSWORD=vscode  # change locally if exposing beyond localhost

# Add new vars only when referenced in code; keep this file in sync.

# Ollama local LLM (gemma:2b default)
OLLAMA_MODEL=gemma:2b
OLLAMA_HOST=127.0.0.1
OLLAMA_PORT=11434
# Alternative quantized variant after creation:
# OLLAMA_MODEL=gemma2b-q4
OLLAMA_URL=http://127.0.0.1:11434

# llama.cpp optional local server (Phi-3, etc.)
# If running: ./llama.cpp --model /models/phi-3-mini-4k-instruct-q8_0.gguf --server --port 8080 --api
# Then set:
# LLAMA_CPP_SERVER_URL=http://127.0.0.1:8080
# Optionally keep model path for future direct (non-server) invocation modes
LLAMA_CPP_SERVER_URL=
LLAMA_CPP_MODEL=

# Postgres vector / RAG settings
DATABASE_URL=postgresql://zenglow:examplepassword@localhost:5432/zenglow
PG_EMBED_DIM=768
RAG_TOP_K_DEFAULT=5

# Optional advanced config (uncomment as needed)
# RAG_TOP_K_MAX=200
# RAG_FUSION_LTR_WEIGHT=0.6
# RAG_FUSION_CONCEPTUAL_WEIGHT=0.4
# REDIS_SSL=false

# GitHub Personal Access Token (scopes: repo). Used by current affairs publish/pull scripts if set.
# Copy to .env (do NOT commit real token). Example scopes: repo
GITHUB_PAT=

# --- Frontend Metrics Dashboard Controls (added Aug 2025) ---
# Disable entire metrics UI panels (chat iframe still accessible)
VITE_DISABLE_METRICS=0
# Disable automatic polling (manual Refresh button only)
VITE_DISABLE_METRICS_POLLING=0
# Poll interval ms (enforced minimum 10000)
VITE_METRICS_POLL_INTERVAL_MS=30000
# Manual refresh cooldown ms
VITE_METRICS_MANUAL_COOLDOWN_MS=5000
# Override metrics endpoint (default /metrics/dashboard)
VITE_METRICS_URL=

# To completely suppress metrics network traffic set:
# VITE_DISABLE_METRICS=1 and VITE_DISABLE_METRICS_POLLING=1
